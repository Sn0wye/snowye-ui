---
title: 'TextInput'
description: 'A component to get user input inside a text field.'
layout: ../../../layouts/MainLayout.astro
---

import TextInputDemo from '../../../demos/TextInputDemo.tsx';
import { HeroContainer } from '../../../components/HeroContainer.tsx';
import { PropsTable } from '../../../components/PropsTable';

A component to get user input inside a text field.

## Anatomy

Mount all pieces together and create your input

```tsx
import { TextInput } from '@snowye-ui/react';

<TextInput.Root>
  <TextInput.LeftIcon>{/* Your Icon */}</TextInput.LeftIcon>
  <TextInput.Input placeholder='Type some text...' />
  <TextInput.RightIcon
    activeIcon={/* Icon when text is visible */}
    inactiveIcon={/* Icon when text not visible */}
  />
</TextInput.Root>;
```

## Example

<HeroContainer client:idle>
  <TextInputDemo client:idle />
</HeroContainer>

## API Reference

### Root

Contains all parts of the TextInput.

<PropsTable
  client:only='react'
  data={[
    {
      name: 'children',
      required: false,
      type: 'ReactNode'
    },
    {
      name: 'error',
      required: false,
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'disabled',
      required: false,
      type: 'boolean',
      default: 'false'
    }
  ]}
/>

### LeftIcon

A feedback icon that sits on the left side of the input field.

<PropsTable
  client:only='react'
  data={[
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: 'Icon shown when the text is visible'
    }
  ]}
/>

### Prefix

A prefix text shown before the input text

<PropsTable
  client:only='react'
  data={[
    {
      name: 'children',
      required: false,
      type: 'string',
      description: 'The text that will be shown before the input text'
    }
  ]}
/>

### Input

The HTML input itself

<PropsTable
  client:only='react'
  data={[
    {
      name: 'children',
      required: false,
      type: 'ReactNode',
      description: 'Icon shown when the text is visible'
    }
  ]}
/>

### RightIcon

A feedback icon/text visibility toggler that sits on the right side of the input field.

<PropsTable
  client:only='react'
  data={[
    {
      name: 'activeIcon',
      required: false,
      type: 'ReactNode',
      description: 'Icon shown when the text is visible'
    },
    {
      name: 'inactiveIcon',
      required: false,
      type: 'ReactNode',
      description: 'Icon shown when the text is not visible'
    }
  ]}
/>
