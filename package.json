{
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "build:docs": "turbo build --filter=snowye",
    "lint": "turbo lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build --filter=!docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.0",
    "turbo": "^1.5.6"
  }
}
