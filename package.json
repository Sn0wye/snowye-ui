{
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "build:docs": "turbo build --filter=centaurus",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^1.5.6"
  }
}
